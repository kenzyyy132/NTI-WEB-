const products = {
    1: "Laptop",
    2: "Phone",
    3: "Tablet"
};


function getProduct(id) {
    return new Promise((resolve, reject) => {
        if (products[id]) {
            resolve(products[id]); // Product found
        } else {
            reject("Error: Product not found."); // Product not found
        }
    });
}

// Example 1: Existing product
getProduct(2)
    .then(product => console.log(product))
    .catch(error => console.log(error));

// Example 2: Invalid product
getProduct(5)
    .then(product => console.log(product))
    .catch(error => console.log(error));
